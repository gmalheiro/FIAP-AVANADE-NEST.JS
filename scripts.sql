create database if not exists avanade_fiap_out22;
use avanade_fiap_out22;

show tables;

desc users;
/* At first time it shouldn't appear nothing at users, just the table structure */

select * from users;